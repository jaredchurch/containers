name: Build and Push DNS Server Image

on:
  push:
    paths:
      - 'dns-server/**'
      - '.github/workflows/build-dns-server.yml'
  workflow_dispatch:
    inputs:
      dev_mode:
        description: 'Build as dev image only (dev tag) even on the main branch'
        required: false
        default: false
        type: boolean

jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_build-container-dev.yml
    with:
      image_name: dns-server
      context: ./dns-server
      dockerfile: ./dns-server/Dockerfile.dns-server
      dev_mode: ${{ github.event_name == 'workflow_dispatch' && inputs.dev_mode }}
    secrets: inherit
